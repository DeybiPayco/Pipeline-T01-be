spring.datasource.url=jdbc:sqlserver://52.5.121.14:1433;databaseName=FARMAGRO_SA;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=SqlPass_2026!
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=none
spring.main.allow-bean-definition-overriding=true
spring.jpa.open-in-view=false

# Swagger / SpringDoc
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

server.port=8080

# Subida de archivos — máximo 5 MB por imagen
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Carpeta donde se guardan las imágenes de productos
# Las imágenes se sirven en: http://HOST:8080/images/products/ARCHIVO
# En desarrollo: ruta relativa al proyecto
# En producción (JAR/Docker): usar ruta absoluta como /app/images/products
app.upload.dir=uploads/images/products

# ── Configuración de correo (Gmail SMTP) ─────────────────────────────────────
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:farmagro828@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:bkpianojdakjdjsu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
